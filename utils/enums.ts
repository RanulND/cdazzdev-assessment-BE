export enum ROLES {
    STUDENT = "STUDENT",
    ADMIN = "ADMIN"
}

export enum COLLECTIONS {
    USERS = "users",
    COURSES = "courses",
    ENROLLMENTS = "enrollments"
}

export enum courseModes {
    ONLINE = "online",
    PHYSICAL = "physical"
}